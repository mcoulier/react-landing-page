import React from "react";
import { AboutContainer, SectionTitle } from "./styled";

export default function About() {
  return (
    <AboutContainer>
      <SectionTitle>About</SectionTitle>
      
    </AboutContainer>
  );
}
